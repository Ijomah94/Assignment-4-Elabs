let myArr = [17, 20, 23, 30];
console.log(myArr.map(function(value){
  return value +=5;
}));

// The map function creates new variables , it is used to execute an action simultanoneously on each item in an array

// The map function is performed to give transformation on a copy of each element
